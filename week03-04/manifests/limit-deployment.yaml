apiVersion: apps/v1
kind: Deployment
metadata:
  name: limit-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: limit-deployment
  template:
    metadata:
      labels:
        app: limit-deployment
    spec:
      containers:
      - name: app
        image: nginx
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"