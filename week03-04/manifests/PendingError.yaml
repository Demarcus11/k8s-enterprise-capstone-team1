apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
spec:
  tolerations:
  - key: "test"
    operator: "Equal"
    value: "noschedule"
    effect: "NoSchedule"

  containers:
  - name: nginx
    image: nginx