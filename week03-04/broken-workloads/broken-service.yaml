apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
spec:
  selector:
    app: broken-service # label must match deployment label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
