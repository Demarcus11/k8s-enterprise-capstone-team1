apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
spec:
  selector:
    app: demo-server # label must match deployment label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
