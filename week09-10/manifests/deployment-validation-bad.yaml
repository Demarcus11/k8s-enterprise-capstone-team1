apiVersion: apps/v1
kind: Deployment
metadata:
  name: invalid-app
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: invalid-app
  template:
    metadata:
      labels:
        app: invalid-app
    spec:
      containers:
      - name: app
        image: nginx:latest