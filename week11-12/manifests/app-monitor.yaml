apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dev-app-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      monitoring: enabled
  namespaceSelector:
    matchNames:
      - dev
  endpoints:
    - port: web
      interval: 15s
